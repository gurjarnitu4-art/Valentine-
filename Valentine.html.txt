<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For You ❤️</title>

<style>
body {
    margin: 0;
    font-family: 'Georgia', serif;
    background: linear-gradient(#1b1b1b, #000);
    color: #fff;
    text-align: center;
    overflow: hidden;
}

.page {
    display: none;
    height: 100vh;
    padding: 30px;
    position: relative;
}

.active {
    display: block;
}

/* FLOATING HEARTS */
.heart {
    position: absolute;
    bottom: -20px;
    font-size: 20px;
    animation: floatUp 6s linear infinite;
    color: pink;
    opacity: 0.8;
}

@keyframes floatUp {
    0% {transform: translateY(0) scale(1);}
    100% {transform: translateY(-110vh) scale(1.4);}
}

/* PAGE 1 */
#page1 {
    display: flex;
    justify-content: center;
    align-items: center;
}

.open-btn {
    font-size: 28px;
    padding: 20px 40px;
    border-radius: 50px;
    border: none;
    background: crimson;
    color: white;
    cursor: pointer;
    animation: pulse 1.5s infinite;
}

@keyframes pulse {
    0% {transform: scale(1);}
    50% {transform: scale(1.08);}
    100% {transform: scale(1);}
}

/* PAGE 2 */
.poem {
    margin-top: 60px;
    font-size: 20px;
    line-height: 1.8;
}

.options {
    margin-top: 40px;
    position: relative;
}

.options button {
    margin: 20px;
    padding: 14px 30px;
    font-size: 18px;
    border-radius: 30px;
    border: none;
    cursor: pointer;
}

.yes {
    background: hotpink;
    color: white;
}

.runaway {
    position: absolute;
}

/* PAGE 3 */
#page3 img {
    max-width: 90%;
    border-radius: 20px;
    margin-top: 30px;
    box-shadow: 0 0 30px rgba(255,105,180,0.6);
}

.caption {
    margin-top: 20px;
    font-size: 22px;
}
</style>
</head>

<body>

<!-- PAGE 1 -->
<div id="page1" class="page active">
    <button class="open-btn" onclick="goToPage(2)">Open Me 💌</button>
</div>

<!-- PAGE 2 -->
<div id="page2" class="page">
    <div class="poem">
        when i saw u for the very first time<br>
        U made me feel so much alive<br>
        Oh, how much I longed u to be mine<br>
        I would love u all my life<br>
        Lets go somewhere nice to dine<br>
        And have a glass of wine,<br>
        <b>Will u be my valentine?</b>
    </div>

    <div class="options">
        <button class="yes runaway" id="runBtn">Yes 💖</button>
        <button class="yes" onclick="goToPage(3)">Yes ×2 💞</button>
    </div>
</div>

<!-- PAGE 3 -->
<div id="page3" class="page">
    <audio autoplay loop>
        <source src="gehra_hua.mp3" type="audio/mpeg">
    </audio>

    <img src="ghibli_us.png" alt="Us">
    <div class="caption">
        Always you. Always us. 🌸
    </div>
</div>

<script>
function goToPage(n) {
    document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
    document.getElementById('page' + n).classList.add('active');
}

/* RUNAWAY YES BUTTON */
const runBtn = document.getElementById("runBtn");
runBtn.addEventListener("mouseover", () => {
    const x = Math.random() * 250 - 120;
    const y = Math.random() * 120;
    runBtn.style.transform = `translate(${x}px, ${y}px)`;
});

/* HEART GENERATOR */
function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}
setInterval(createHeart, 400);
</script>

</body>
</html>